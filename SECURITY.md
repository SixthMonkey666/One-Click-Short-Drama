# Security Policy

## Supported Version

项目当前只维护默认分支的最新版本。尚未发布独立的长期支持版本。

## Reporting a Vulnerability

请通过仓库托管平台提供的私密安全报告功能联系维护者。若该功能不可用，请使用维护者在项目主页公开的安全联系方式。

报告中请包含：

- 受影响的版本或提交
- 漏洞影响和攻击前提
- 最小复现步骤或概念验证
- 建议修复方式（如有）

请勿在公开 Issue、Pull Request、日志或截图中披露未修复漏洞、真实 API Key、私有 Prompt、生成素材或本地数据库。

维护者会尽快确认报告、评估影响并协调修复和披露时间。在修复可用前，请避免公开利用细节。

## Security Boundaries

- `.env`、`data/`、模型权重和 `.streamlit/secrets.toml` 均应保持在版本控制之外。
- 应用默认本地运行，但启用 Langfuse 或自定义云端 Provider 后，数据可能发送到第三方服务。
- Docker 服务没有内置身份认证或 TLS。不要直接暴露到不受信任网络；应在前置代理中配置访问控制和 HTTPS。
- 导出会过滤常见敏感键，但不能替代发布前的人工检查。
- 模型输出、上传文件和外部 API 响应均应视为不可信输入。

## Credential Response

若密钥被提交、写入日志或导出：

1. 立即在对应服务撤销并轮换密钥。
2. 删除公开内容中的敏感值。
3. 检查访问日志和异常调用。
4. 必要时清理 Git 历史；仅删除最新文件并不能消除历史泄露。
